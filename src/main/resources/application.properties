# THYMELEAF (ThymeleafAutoConfiguration)
# 开启模板缓存（默认值： true ）
spring.thymeleaf.cache=false
# 检查模板是否存在，然后再呈现
spring.thymeleaf.check-template=true
# 检查模板位置是否正确（默认值 :true ）
spring.thymeleaf.check-template-location=true
#Content-Type 的值（默认值： text/html ）
spring.thymeleaf.content-type=text/html
# 开启 MVC Thymeleaf 视图解析（默认值： true ）
spring.thymeleaf.enabled=true
# 模板编码
spring.thymeleaf.encoding=UTF-8
# 要被排除在解析之外的视图名称列表，⽤逗号分隔
spring.thymeleaf.excluded-view-names=
# 要运⽤于模板之上的模板模式。另⻅ StandardTemplate-ModeHandlers( 默认值： HTML5)
spring.thymeleaf.mode=HTML
# 在构建 URL 时添加到视图名称前的前缀（默认值： classpath:/templates/ ）
spring.thymeleaf.prefix=classpath:/templates/
# 在构建 URL 时添加到视图名称后的后缀（默认值： .html ）
spring.thymeleaf.suffix=.html
#下面这些内容是为了让MyBatis映射
#指定Mybatis的Mapper文件
mybatis.mapper-locations=classpath:mappers/*.xml
#指定Mybatis的实体目录
mybatis.type-aliases-package=com.example.layuidemo.entity


# 应用服务 WEB 访问端口
#server.port=8080
server.port=${PORT:8080}  # Railway 会设置 PORT 环境变量
server.shutdown=graceful

## 数据库JDBC URL，确保数据库名称（mydatabase）和地址端口正确
#spring.datasource.url=jdbc:mysql://localhost:3307/school
## 数据库用户名
#spring.datasource.username=root
## 数据库密码
#spring.datasource.password=root
## JDBC驱动类（高版本Spring Boot通常可自动识别，可省略）
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=jdbc:mysql://${MYSQLHOST:mysql.railway.internal}:${MYSQLPORT:3306}/${MYSQLDATABASE:railway}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:NjsBZnCJqgMSiJxEAHmwkfOawPsMiiAd}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# 优雅关闭的超时时间
spring.lifecycle.timeout-per-shutdown-phase=5s

spring.devtools.restart.enabled=true
# 解决devtools热部署可能导致的类加载问题
# 每2秒检查一次文件变化
spring.devtools.restart.poll-interval=2000ms
# 检测到变化后，等待1秒再重启（确保连续修改完成）
spring.devtools.restart.quiet-period=1000ms

#spring.devtools.restart.exclude=
#static/**,
#public/**,
#templates/**
